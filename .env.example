# VoiceDNA Environment Configuration (Optional)
# You can also configure these dynamically inside the web app Settings modal.

# OpenAI API Key (if using OpenAI provider)
OPENAI_API_KEY=

# Ollama Endpoint (if using local Ollama provider)
OLLAMA_BASE_URL=http://localhost:11434
